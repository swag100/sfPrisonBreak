#pragma once
#include <string>

namespace constants {
	const std::string TITLE = "SFML works, CONSTANTS FILE!";

    const int BUFFER_WIDTH = 320;
    const int BUFFER_HEIGHT = 240;

    const int ZOOM = 2;
}